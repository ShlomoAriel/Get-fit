import {connect} from 'react-redux';
import R from 'ramda';
import Workout from '../display/Workout';

function mapStateToProps(state) {
    return {
    }
}

function mapDispatchToProps(dispatch) {
    return {
    }
}

export default connect( mapStateToProps, mapDispatchToProps )(Workout)

