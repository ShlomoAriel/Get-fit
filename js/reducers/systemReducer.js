import * as types from 'actions/actionTypes';
import R from 'ramda';

const initialState = {

}

export default function(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
